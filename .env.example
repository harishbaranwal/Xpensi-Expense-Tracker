
# ================================================================
# EXAMPLE FILE FOR ENVIRONMENT VARIABLES
# ================================================================
#
# INSTRUCTIONS:
# 1. For DEVELOPMENT: Copy this file as '.env.local'
#    cp .env.example .env.local
#
# 2. For PRODUCTION: Copy this file as '.env.production'
#    cp .env.example .env.production
#
# 3. Edit the variables according to your environment
#
# IMPORTANT: Never commit .env.* files to the repository (they are in .gitignore)
# ================================================================

# Base configuration
SECRET_KEY=your-secret-key-here
DEBUG=True
DJANGO_SETTINGS_MODULE=config.settings.local

# PostgreSQL Database
DB_NAME=gastos_hormiga_dev
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# For production
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# Trusted origins for CSRF (production with HTTPS)
CSRF_TRUSTED_ORIGINS=https://your-domain.com,http://127.0.0.1

# Email configuration (optional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=noreply@gastoshormiga.com

# Security settings (production)
SECURE_SSL_REDIRECT=False
CSRF_COOKIE_SECURE=False
SESSION_COOKIE_SECURE=False

# ================================================================
# IMPORTANT NOTES:
# ================================================================
# 
# DEVELOPMENT (.env.local):
# - DEBUG=True
# - SECRET_KEY can be less secure
# - DB_HOST=localhost
# - DB_PASSWORD can be simple
#
# PRODUCTION (.env.production):  
# - DEBUG=False
# - SECRET_KEY must be very secure (50+ characters)
# - ALLOWED_HOSTS must include your domain
# - CSRF_TRUSTED_ORIGINS must include your domain with https://
# - DB_PASSWORD must be very secure
# - Configure EMAIL_* for sending emails
# - SECURE_SSL_REDIRECT=True if using HTTPS
# - CSRF_COOKIE_SECURE=True if using HTTPS
# - SESSION_COOKIE_SECURE=True if using HTTPS
#
# SERVICE ACCESS:
# Development: 
#   - Django: http://localhost:8000
#   - Admin: http://localhost:8000/admin  
# Production:
#   - Django: https://your-domain.com
#   - Admin: https://your-domain.com/admin
#
# (This file is not needed for local-only development. Please delete it.)
# ================================================================